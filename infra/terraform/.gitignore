# Local Terraform working state and provider plugin cache; never commit
# these, and never commit terraform.tfvars since it holds real credentials
# and the webhook secret.
.terraform/
.terraform.tfstate.lock.info
*.tfstate
*.tfstate.*
terraform.tfvars
crash.log
crash.*.log
*.tfplan
